

====== Notebook: combine_bg_indicators.ipynb ======
Imports: import pandas as pd, import geopandas as gpd, import matplotlib.pyplot as plt, import cartopy.crs as ccrs, import contextily as ctx
Code snippet: import pandas as pd import geopandas as gpd import matplotlib.pyplot as plt import cartopy.crs as ccrs import contextily as ctx...
Code snippet: indicators = pd.read_csv('selected_counties_block_group_data.csv')...
Code snippet: indicators.state.unique()...
Code snippet: state_fips = {     'NY': '36',     'DC': '11',     'MD': '24',     'VA': '51',     'IL': '17',} geom_dir = 'blockgroups_geoms' gdf_states = {} for state, fips in state_fips.items():     print(state, f...
Code snippet: fig, ax = plt.subplots(figsize=(10, 10)) combined_geodata_projected = combined_geodata.to_crs(epsg=3857)  combined_geodata_projected.plot(ax=ax) ctx.add_basemap(ax, source=ctx.providers.CartoDB.Positr...
Code snippet: indicators['GEOID'].astype(str)...
Code snippet: indicators...
Imports: import pandas as pd
Code snippet: import pandas as pd indicators['GEOID'] = indicators['GEOID'].astype(str) final_geodata = combined_geodata.merge(indicators, left_on='GEOID', right_on='GEOID', how='left') final_geodata.drop(columns=[...
Code snippet: final_geodata = final_geodata.to_crs(epsg=3857)  fig, ax = plt.subplots(figsize=(20, 10)) final_geodata.plot(     column='median_household_income',      cmap='OrRd',      legend=True,      ax=ax,     ...
Code snippet: final_geodata.to_file('blockgroups_indicators.geojson', driver='GeoJSON')...


====== Notebook: combine_lst.ipynb ======
Imports: import matplotlib.pyplot as plt, import rasterio as rio, import cartopy.crs as ccrs, import geopandas as gpd, import rioxarray as rioxr, import xarray as xr, import os, import numpy as np
Code snippet: import matplotlib.pyplot as plt import rasterio as rio import cartopy.crs as ccrs import geopandas as gpd import rioxarray as rioxr import xarray as xr import os import numpy as np...
Markdown Header: # Landsat Surface Temperature Processing and Analysis
Code snippet: CITIES = ['NYC', 'Chicago', 'DC'] WORKDIR = 'C:/Users/lortizur/Documents/projects/Transit-Stations-Heat/landsat/' OUTPUT_DIR = 'city_mean_temperatures' ST_SCALE_FACTOR = 0.00341802 ST_OFFSET = 149.0 K...
Markdown: ---  ## 2. Define Constants and Configuration Parameters  This section establishes the configuration parameters for the analysis:  - **Study cities**: NYC, Chicago, and Washington DC - **Conversion co...
Code snippet: st_folders = {city: f'{WORKDIR}{city}' for city in CITIES} st_files = {city: [f for f in os.listdir(st_folders[city]) if f.endswith('B10.TIF')] for city in CITIES} print(f"Found files: {', '.join([f'{...
Markdown: ---  ## 3. Organize Data Directory Structure  Establish the directory structure and identify available surface temperature files for each city. The analysis focuses on files containing Band 10 thermal...
Definitions: def create_clear_mask(qa_array):
Code snippet: def create_clear_mask(qa_array):     """     Create a mask for clear, non-water pixels from Landsat QA_PIXEL     Returns True for clear, non-water pixels     """     # Convert to integer for bitwise o...
Markdown: ---  ## 4. Quality Masking Function  Define a function to create pixel-level quality masks using Landsat Collection 2 QA_PIXEL bit flags. This function filters pixels based on:  ### Quality Assessment...
Code snippet: city_mean_temps = {} print("Processing cities...") for city in CITIES:     st_file_list = st_files[city]     city_folder = st_folders[city]     masked_arrays = []     processed_count = 0     error_cou...
Markdown: ---  ## 5. Process Surface Temperature Data  This section performs the core data processing workflow for each city:  ### Processing Steps  1. **Load thermal data**: Read surface temperature (ST_B10.TI...
Code snippet: fig, axes = plt.subplots(1, 3, figsize=(18, 6), subplot_kw={'projection': ccrs.PlateCarree()}) for i, city in enumerate(CITIES):     ax = axes[i]     # Plot the mean temperature data     im = city_mea...
Markdown: ---  ## 6. Visualize Mean Surface Temperatures  Create a three-panel figure displaying the mean surface temperature distribution for each city. The visualization uses:  - **Projection**: Plate Carrée ...
Code snippet: os.makedirs(OUTPUT_DIR, exist_ok=True) print(f"Saving to '{OUTPUT_DIR}' directory...") saved_files = [] for city in CITIES:     if city not in city_mean_temps:         print(f"  ⚠️  {city}: No data av...
Markdown: ---  ## 7. Export Processed Data  Export the mean surface temperature rasters to GeoTIFF format for use in subsequent analyses or GIS applications.  ### Output Specifications  - **Format**: Cloud-Opti...
Imports: import json, from datetime import datetime
Code snippet: import json from datetime import datetime print("Exploring JSON metadata structure...\n") test_files = {     'NYC': 'LC08_CU_029007_20240601_20240615_02.json',     'Chicago': 'LC08_CU_021008_20240607_...
Markdown: ---  ## 8. Metadata Exploration (Optional)  Examine the structure and content of Landsat ARD JSON metadata files. This section demonstrates how to:  - Navigate the nested JSON structure containing sce...


====== Notebook: download_acs.ipynb ======
Imports: import pandas as pd, import geopandas as gpd, from census import Census, import time
Code snippet: import pandas as pd import geopandas as gpd from census import Census import time...
Code snippet: c = Census(key=None)  # Replace None with your actual API key if needed counties_to_query = [     # Virginia     ('51', '013', 'Arlington County, VA'),     ('51', '059', 'Fairfax County, VA'),     ('5...
Code snippet: all_data = [] for state_fips, county_fips, county_name in counties_to_query:     try:         # Define the geography for the API call         geo = {'for': 'block group:*', 'in': f'state:{state_fips} ...
Code snippet: print("\n--- All counties processed. Combining data... ---") if all_data:     final_df = pd.concat(all_data, ignore_index=True)     final_df = final_df.rename(columns=variables_to_get)     numeric_col...
Code snippet: len(final_df['GEOID'].loc[0])...
Code snippet: gdf = gpd.read_file('blockgroups_geoms/tl_2023_51_bg.shp')...
Code snippet: len(gdf['GEOID'].loc[0])...


====== Notebook: explore_qa.ipynb ======
Imports: import rioxarray as rxr, import xarray as xr, import matplotlib.pyplot as plt
Code snippet: import rioxarray as rxr import xarray as xr import matplotlib.pyplot as plt...
Code snippet: rds_qa = rxr.open_rasterio('landsat/Chicago/LC08_CU_021007_20240614_20240704_02_QA_PIXEL.TIF', masked=True)...
Code snippet: rds_qa.plot()...
Imports: import numpy as np
Code snippet: import numpy as np print("QA pixel value statistics:") print(f"Min: {rds_qa.min().values}") print(f"Max: {rds_qa.max().values}") print(f"Shape: {rds_qa.shape}") unique_vals = rds_qa.values.flatten() u...
Definitions: def create_clear_mask(qa_array):
Code snippet: def create_clear_mask(qa_array):     """     Create a mask for clear, non-water pixels from Landsat QA_PIXEL     Returns True for clear, non-water pixels     """     # Convert to integer for bitwise o...
Code snippet: fig, axes = plt.subplots(1, 2, figsize=(15, 6)) rds_qa.plot(ax=axes[0], cmap='viridis') axes[0].set_title('Original QA_PIXEL Layer') axes[0].set_xlabel('X') axes[0].set_ylabel('Y') clear_mask.plot(ax=...
Code snippet: st_file = 'landsat/Chicago/LC08_CU_021007_20240614_20240704_02_ST_B10.TIF' try:     # Load surface temperature data     surface_temp = rxr.open_rasterio(st_file, masked=True)     # Apply the clear pix...
Code snippet: fig, axes = plt.subplots(2, 2, figsize=(15, 12)) st_celsius = (surface_temp * 0.00341802 + 149.0) - 273.15 masked_st_celsius = st_celsius.where(clear_mask) st_celsius.plot(ax=axes[0,0], cmap='RdYlBu_r...
Definitions: def categorize_temperature(temp_array):
Code snippet: print("=== SURFACE TEMPERATURE ANALYSIS WITH QA MASKING ===\n") st_celsius = (surface_temp * 0.00341802 + 149.0) - 273.15 masked_st_celsius = st_celsius.where(clear_mask) clear_temps = masked_st_celsi...


====== Notebook: lst_urbanparameters.ipynb ======
Imports: import geopandas as gpd, import pandas as pd, import numpy as np, import rasterio, from rasterstats import zonal_stats, import matplotlib.pyplot as plt, import seaborn as sns, import os, import warnings
Code snippet: import geopandas as gpd import pandas as pd import numpy as np import rasterio from rasterstats import zonal_stats import matplotlib.pyplot as plt import seaborn as sns import os import warnings warni...
Code snippet: shapefile_path = "Network Density/Walksheds_with_NetworkDensity.shp" uMORPH_dir = "uMORPH" building_height_raster = os.path.join(uMORPH_dir, "merged_mean_building_height_100m_wgs.tif") plan_area_raste...
Code snippet: walksheds_wgs84 = walksheds.to_crs(epsg=4326) print("Extracting mean building height...") bh_stats = zonal_stats(     vectors=walksheds_wgs84,     raster=building_height_raster,     stats=['median'], ...
Definitions: def get_city_background_lst(raster_path):
Code snippet: all_walksheds_lst = [] def get_city_background_lst(raster_path):     # Load raster and get background temp, masking nodata     with rasterio.open(raster_path) as src:         data = src.read(1)       ...
Code snippet: nlcd_raster_files = {     "NYC": "NLCD_imperviousFraction/NYC/Annual_NLCD_FctImp_2024_CU_C1V1_ffd3767f-a804-4fd8-a912-5d2ef49a1bd7.tiff",     "Chicago": "NLCD_imperviousFraction/Chicago/Annual_NLCD_Fc...
Imports: import scipy.stats as stats
Definitions: def robust_annotate(x, y, **kwargs):
Code snippet: import scipy.stats as stats cols_to_plot = ['mean_building_height', 'plan_area_fraction', 'impervious_fraction', 'D3A', 'D3APO'] df_melted = walksheds_processed.melt(     id_vars=['city', 'station_na'...


====== Notebook: metro_locations_analysis.ipynb ======
Imports: import geopandas as gpd, import matplotlib.pyplot as plt, import contextily as ctx
Code snippet: import geopandas as gpd import matplotlib.pyplot as plt import contextily as ctx...
Code snippet: gdf= gpd.read_file('station_temperature_statistics.geojson')...
Code snippet: top10_by_city = gdf.groupby('city').apply(lambda x: x.nlargest(10, 'median_temp')).reset_index(drop=True) display(top10_by_city[['city', 'station_name', 'median_temp']])...
Code snippet: cities = ['Chicago', 'DC', 'NYC'] fig, axes = plt.subplots(1, 3, figsize=(18, 6)) for ax, city in zip(axes, cities):     city_data = top10_by_city[top10_by_city['city'] == city]     # Plot base map fo...
Code snippet: fig, axes = plt.subplots(1, 3, figsize=(18, 6)) cities = ['Chicago', 'NYC', 'DC'] for ax, city in zip(axes, cities):     city_data = gdf[gdf['city'] == city]     # Plot all stations colored by median ...
Code snippet: gdf_subset = gdf[gdf.columns[:8]]...
Code snippet: gdf_subset = gpd.GeoDataFrame(gdf_subset, geometry=gdf.geometry, crs=gdf.crs)...
Code snippet: gdf_subset.to_file('station_walkshed_temps.geojson', driver='GeoJSON')...
Code snippet: gdf_subset[gdf['city']=='DC'].plot()...


====== Notebook: plot_lst.ipynb ======
Imports: import rasterio as rio, from rasterio.features import geometry_mask, from rasterio.warp import reproject, Resampling, calculate_default_transform, from rasterio.windows import from_bounds, import geopandas as gpd, import matplotlib.pyplot as plt, import cartopy.crs as ccrs, import numpy as np, from scipy import stats, from scipy.optimize import curve_fit, import os
Code snippet: import rasterio as rio from rasterio.features import geometry_mask from rasterio.warp import reproject, Resampling, calculate_default_transform from rasterio.windows import from_bounds import geopanda...
Code snippet: CONFIG = {     "cities": ["NYC", "Chicago", "DC"],     "paths": {         "input_template": "stitched_city_temperatures/{city}_subset_surface_temperature.tif",         "station_stats_template": "stati...
Definitions: class Visualizer:
Code snippet: class Visualizer:     def __init__(self, config):         self.config = config         self.cities = config["cities"]     def load_city_data(self, city):         import rasterio as rio         from ra...
Definitions: class Visualizer(Visualizer):
Code snippet: class Visualizer(Visualizer):     def create_scatter_analysis(self):         """         Generate scatter plots of LST Anomalies vs Plan Area Fraction, Building Height,          and Network Density us...
Code snippet: visualizer.create_dashboard()...
Code snippet: visualizer.create_scatter_analysis()...
Imports: import geopandas as gpd, from rasterstats import zonal_stats, import numpy as np
Code snippet: import geopandas as gpd from rasterstats import zonal_stats import numpy as np city = "NYC" data, extent, crs, transform = visualizer.load_city_data(city) gdf = gpd.read_file('Network Density/Walkshed...
Code snippet: city = "NYC" lst_data, extent, lst_crs, lst_transform = visualizer.load_city_data(city) lst_height, lst_width = lst_data.shape umorph_path = visualizer.config['paths']['umorph_path'] with rio.open(umo...
Code snippet: fig, axes = plt.subplots(1, 3, figsize=(20, 6)) for i, city in enumerate(visualizer.cities):     ax = axes[i]     try:         # Load LST Data         lst_data, extent, _, _ = visualizer.load_city_dat...
Code snippet: fig, axes = plt.subplots(2, 3, figsize=(18, 10)) variables = [     {         "path_key": "umorph_path",         "label": "Plan Area Fraction",         "bins": 50,         "range": (0, 1)     },     { ...
Code snippet: morph_flat.min()...


====== Notebook: subset_lst.ipynb ======
Imports: import os, import rasterio as rio, from rasterio.mask import mask, import geopandas as gpd, import matplotlib.pyplot as plt, from shapely.geometry import box, import numpy as np
Code snippet: import os import rasterio as rio from rasterio.mask import mask import geopandas as gpd import matplotlib.pyplot as plt from shapely.geometry import box import numpy as np CONFIG = {     "cities": ["N...
Definitions: def crop_raster(city, config):
Code snippet: def crop_raster(city, config):     """     Crops the LST raster to the bounding box of the stations plus a buffer.     """     # Paths     lst_path = config['paths']['lst_template'].format(city=city) ...
Code snippet: fig, axes = plt.subplots(1, 3, figsize=(11, 4)) im = None for ax, city in zip(axes, CONFIG['cities']):     if city in results:         data, extent, stations = results[city]         # Plot LST Subset ...


====== Notebook: test_tile_combine.ipynb ======
Imports: import os, import glob, import numpy as np, import xarray as xr, import rioxarray as rxr, from rioxarray.merge import merge_arrays, import matplotlib.pyplot as plt, from pathlib import Path, import warnings
Code snippet: import os import glob import numpy as np import xarray as xr import rioxarray as rxr from rioxarray.merge import merge_arrays import matplotlib.pyplot as plt from pathlib import Path import warnings w...
Definitions: def create_clear_mask(qa_array):
Code snippet: def create_clear_mask(qa_array):     """     Create a mask for clear, non-water pixels from Landsat QA_PIXEL     Returns True for clear, non-water pixels     Landsat Collection 2 QA_PIXEL bit definiti...
Definitions: def extract_tile_id(filename):, def group_files_by_tile(files):
Code snippet: def extract_tile_id(filename):     """     Extract tile ID from Landsat filename     Example: LC08_CU_021007_20240614_20240704_02_ST_B10.TIF -> 021007     """     parts = filename.split('_')     if le...
Definitions: def process_tile(st_files, qa_files, tile_id):
Code snippet: def process_tile(st_files, qa_files, tile_id):     """     Process a single tile: apply QA masking and calculate temporal mean     """     print(f"    Processing tile {tile_id}...")     print(f"      ...
Definitions: def process_city(city_name):
Code snippet: cities = ['NYC', 'Chicago', 'DC'] landsat_dir = 'landsat' output_dir = 'stitched_city_temperatures' os.makedirs(output_dir, exist_ok=True) def process_city(city_name):     """     Process all tiles fo...
Code snippet: print("🚀 Starting multi-tile surface temperature processing...") print("=" * 60) city_results = {} for city in cities:     result = process_city(city)     if result is not None:         city_results[c...
Code snippet: if city_results:     print(f"\n📊 Creating visualization for {len(city_results)} cities...")     # Create subplot layout     n_cities = len(city_results)     fig, axes = plt.subplots(1, n_cities, figsi...


====== Notebook: walkshed_analysis_st.ipynb ======
Imports: import geopandas as gpd, import pandas as pd, import osmnx as ox, import networkx as nx, from shapely.geometry import Polygon, MultiPolygon, Point, import matplotlib.pyplot as plt, import contextily as cx, from tqdm import tqdm, import numpy as np, import warnings, import rasterio, from rasterio.mask import mask, from rasterio.warp import calculate_default_transform, reproject, Resampling
Code snippet: import geopandas as gpd import pandas as pd import osmnx as ox import networkx as nx from shapely.geometry import Polygon, MultiPolygon, Point import matplotlib.pyplot as plt import contextily as cx f...
Code snippet: cities = {     'Chicago': gpd.read_file('metrolocs/Chicago.geojson'),     'DC': gpd.read_file('metrolocs/DC.geojson'),     'NYC': gpd.read_file('metrolocs/NYC.geojson') } temp_files = {     'Chicago':...
Code snippet: selected_stations = {} for city, gdf in cities.items():     selected_stations[city] = gdf.copy()     print(f"{city}: {len(gdf)} stations selected for analysis") print(f"\nTotal stations across all cit...
Definitions: def calculate_walksheds(stations, walk_speed_mps=1.25, trip_time_seconds=600):
Code snippet: def calculate_walksheds(stations, walk_speed_mps=1.25, trip_time_seconds=600):     """     Calculate 10-minute walksheds for a set of transit stations.     Parameters:     - stations: GeoDataFrame of ...
Code snippet: city_walksheds = {} city_stations_utm = {} city_utm_crs = {} for city, stations in selected_stations.items():     print(f"Calculating walksheds for {city}...")     walksheds, stations_utm, utm_crs = c...
Code snippet: print("💾 Saving all walksheds to GeoJSON file...") all_walksheds_data = [] for city, walksheds_gdf in city_walksheds.items():     stations_utm = city_stations_utm[city]     utm_crs = city_utm_crs[city...
Code snippet: fig, axes = plt.subplots(1, 3, figsize=(20, 10)) fig.suptitle('10-Minute Walksheds from Selected Metro Stations', fontsize=16) for ax, city in zip(axes, selected_stations.keys()):     # Use pre-calcul...
Code snippet: fig, axes = plt.subplots(1, 3, figsize=(20, 10)) fig.suptitle('Clipped Surface Temperature within 10-Minute Walksheds', fontsize=16) for ax, city in zip(axes, selected_stations.keys()):     # Use pre-...
Definitions: def calculate_temperature_stats_for_station(station_geom, walkshed_geom, walkshed_crs, temp_raster_path):
Code snippet: def calculate_temperature_stats_for_station(station_geom, walkshed_geom, walkshed_crs, temp_raster_path):     """     Calculate temperature statistics for a single station's walkshed.     Parameters: ...
Code snippet: station_temperature_gdf.to_file('station_temperature_statistics.geojson', driver='GeoJSON')...
Code snippet: station_temperature_gdf = gpd.read_file('station_temperature_statistics.geojson')...
Imports: import json, from branca.colormap import LinearColormap
Definitions: def create_interactive_station_map(gdf):
Code snippet: try:     import folium     from folium import plugins except ImportError:     print("Installing folium...")     import subprocess     import sys     subprocess.check_call([sys.executable, "-m", "pip",...
Code snippet: interactive_map.save('station_temperature_map.html')...
Imports: import matplotlib.pyplot as plt
Code snippet: import matplotlib.pyplot as plt plt.rcParams['font.family'] = 'sans-serif' plt.rcParams['font.sans-serif'] = ['Figtree'] fig, axes = plt.subplots(1, 3, figsize=(12, 4)) cities_to_plot = ['NYC', 'DC', ...
